{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^16.0.2",
    "purescript": "^0.15.12",
    "purescript-psa": "^0.8.2",
    "purs-tidy": "^0.10.0",
    "rimraf": "^5.0.5"
  }
}
